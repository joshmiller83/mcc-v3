diff -ruN a/components/hero-card/hero-card.component.yml b/components/hero-card/hero-card.component.yml
--- a/components/hero-card/hero-card.component.yml	2026-03-23 12:36:03.000000000 +0000
+++ b/components/hero-card/hero-card.component.yml	2026-07-31 16:15:15.215739441 +0000
@@ -7,6 +7,11 @@
   required:
     - heading
   properties:
+    subtitle:
+      type: string
+      title: Subtitle Badge
+      examples:
+        - "EST. 1840 · KIRKLIN, INDIANA"
     heading:
       type: string
       title: Heading
@@ -35,3 +40,6 @@
   ctas:
     title: CTAs
     description: "Call-to-action buttons displayed below the hero content."
+  side_media:
+    title: Side Media Slot
+    description: "Optional right-column photo placeholder or media component."
diff -ruN a/components/hero-card/hero-card.twig b/components/hero-card/hero-card.twig
--- a/components/hero-card/hero-card.twig	2026-03-23 12:36:03.000000000 +0000
+++ b/components/hero-card/hero-card.twig	2026-07-31 16:15:15.217739441 +0000
@@ -1,17 +1,21 @@
 {#
   Hero Card component.
 
-  Full-width hero section with a background image, dark overlay, centered
-  heading, description, and CTA buttons via the "ctas" slot.
+  Full-width hero section with a background image, dark overlay, an optional
+  subtitle badge, centered heading, description, and CTA buttons via the
+  "ctas" slot. An optional "side_media" slot renders a second column to the
+  right of the text content.
 
   Desktop: 640px tall, 64px side padding, 72px serif heading.
   Mobile:  auto height, 32px padding all sides, smaller heading.
 
+  @prop subtitle (string) - Optional subtitle badge text, shown above the heading.
   @prop heading (string) - Main heading text.
   @prop description (string) - Supporting body text.
   @prop media (object) - Background image media object.
   @prop heading_level (number) - HTML heading level (1–3). Defaults to 1.
   @slot ctas - Call-to-action buttons displayed below the hero content.
+  @slot side_media - Optional right-column photo placeholder or media component.
 #}
 
 {% set level = heading_level|default(1) %}
@@ -35,7 +39,11 @@
 			</div>
 		{% endif %}
 
-		<div class="hero-card__column">
+		<div class="hero-card__column hero-card__column--left">
+
+			{% if subtitle %}
+				<div class="hero-card__subtitle-badge">{{ subtitle }}</div>
+			{% endif %}
 
 			<div class="hero-card__content">
 				{% if heading %}
@@ -60,5 +68,12 @@
 		{% endif %}
 
 		</div>
+
+		{% if side_media is not empty or logged_in %}
+			<div class="hero-card__column hero-card__column--right">
+				{% block side_media %}
+				{% endblock %}
+			</div>
+		{% endif %}
 	</div>
 </div>
